<Window x:Class="z2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        xmlns:local="clr-namespace:z2"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel>
        <TextBlock Text="Ime studenta" Margin="5"/>
        <TextBox Text="{Binding Student.Ime}" Margin="5"/>
        <TextBlock Text="Prezime studenta" Margin="5"/>
        <TextBox Text="{Binding Student.Prezime}" Margin="5"/>
        <TextBlock Text="Telefon studenta" Margin="5"/>
        <TextBox Text="{Binding Student.Telefon}" Margin="5"/>
        <TextBlock Text="Godina studija" Margin="5"/>
        <TextBox Text="{Binding Student.GodinaStudija}" Margin="5"/>
        <Button Content="Predji u sledecu godinu" Click="Button_Click" Margin="10" Width="200"/>
    </StackPanel>
</Window>
